import { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    absolute: "about next js routing",
  },
};

const Page = () => {
  return <div>About page</div>;
};

export default Page;
